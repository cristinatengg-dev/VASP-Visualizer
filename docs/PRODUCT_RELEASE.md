# EliangMat AI 正式界面与运行说明

2026-09-07。当前完成正式产品界面与独立运行入口，未部署线上。

## 当前查看方式

```sh
npm run build
npm run preview:platform:start
```

打开 http://127.0.0.1:4317/ 。服务直接提供 `dist`，没有 Vite 开发客户端；停止和状态命令为 `preview:platform:stop`、`preview:platform:status`。它与旧开发服务共用 4317 端口，切换前先停止当前服务。再次修改前端后需要重新构建；修改后端后需要重启预览。

首次预览只复制原本机未过期的手机号会话，帮助继续查看界面，不复制挑战码、演练会话或研究/账务数据；这些会话不等于生产短信验证。所有新预览记录进入 `.preview/platform`。`.dev` 原项目、对话、文献和虚拟余额完整保留。生产入口拒绝 `.dev` 和 `.preview` 数据目录，不自动迁移本机身份到线上。

2026-09-07 修复后：已将此前腾讯云短信的必要配置安全复用至本机 `.dev/phone-auth.env`（0600、Git 忽略），预览入口已载入。健康检查 200、无效号码 400，正式页面没有测试码。真实短信送达尚未验收，等待用户提供由其控制的测试号码；配置齐全不等于供应商送达成功。原 QA 会话继续保留供本机查看，不用于生产身份迁移。

## 已去除的界面痕迹

- 官网开发预览脚注和旧隐藏示例表单；保留材料与设备动画的「过程示意」标注。
- 工作区开发环境标记、测试余额、客户 A/B 切换、测试角色和演练项目创建入口。
- 开发验收指南、启动命令、虚构结果样例下载、开发配置指引，替换为面向客户的使用指南和错误提示。
- 模拟充值/支付、计费演练、示例模型价格和未生效的成员邀请草稿入口。源码中的 sandbox 工具与研发测试数据仍可供独立回归，不进入正式界面的可操作流程。
- 默认构建现在进入完整材料研发平台；旧应用保存在 LegacyApp，仅显式设置 `VITE_LEGACY_APP=true` 才使用其原认证与路由。

## 真实服务状态

账号与项目对话、流式过程、权限记忆、文献题录采集、资料导入/原文/证据复核、版本化研发工作流、人工文件回传和 CSV 曲线计算继续可用。外部 Gemini 使用已有服务器密钥及明确数据授权；私密/参与优化独立管理。

充值与结算显示未开通，尚无平台实际收费；当前 Gemini API 费用由供应商账号承担，用量表保留返回 Token。未知用量不显示为零，也不作为确认账单。团队邀请、训练服务和设备/仿真自动接入未被包装成可用功能，按实际连接状态提供操作。

## 正式运行配置

1. 将 `scripts/platform/platform.env.example` 复制到 `.config/platform.env`，填写正式 HTTPS 来源、已审核腾讯云短信参数和模型网关参数。密钥文件应仅限服务账号读取，不入 Git。
2. 安装前后端依赖并构建，运行 `npm run start:platform`。独立入口为 `server/platform.cjs`，不读取旧 `server/.env`，不启动旧支付/计算后台。缺少 HTTPS 来源或短信配置会拒绝启动。
3. 默认存储 `.data/platform`，包含 auth 和 knowledge 子目录。使用持久磁盘并做备份；目前为单进程原子文件存储，勿多副本同时写入同一目录。既有每空间 30 项目、3000 资料、50 MB 状态等容量限制保留，扩容或数据库迁移需另行验证。
4. 在 HTTPS 反向代理后运行，保留客户端 Host，配置的 `ELIANGMAT_ORIGINS` 须与外部完整来源一致。API 代理关闭流式缓冲并允许至少 60 秒请求；生产 cookie 强制 Secure/HttpOnly/SameSite。直接本机 HTTP 不适合正式登录。
5. 容器配置为 `Dockerfile.platform` 和 `docker-compose.platform.yml`，读取同一独立配置，持久化 `.data/platform`，仅将 4320 绑定到主机回环地址，交给 HTTPS 入口转发。镜像上下文排除密钥、开发/预览/客户数据。容器本轮未实际构建或部署。

旧 `docker-compose.yml` / `deploy.sh` 对应此前的后台与账号体系，不能直接当作本平台已完成迁移的发布流程。旧生产用户、短信验证、项目与余额需有明确迁移方案并验证后再切换。本次没有向生产导入任何本机数据，也没有对外发送短信或产生充值。

## 验证记录

89 项自动化测试与 95 项隔离 HTTP 检查通过；新增覆盖正式服务排除虚拟余额/模型、服务层与 HTTP 拒绝演练变更、禁止开发身份、短信验证不回传验证码、Secure cookie、跨来源拦截与客户数据隔离。保留原研发规则、权限记忆、流式中断与 Token 测试。

生产构建、TypeScript、相关前端 ESLint、官网脚本语法检查通过。运行中的首页、对话、项目、账号、指南均返回生产静态入口，没有 `/@vite/client`；开发 API 与虚构样例均 404，未配置短信时明确 503 且无测试码。

浏览器实际验收暂因 Mac 锁屏未完成，已请求解锁；不将 HTTP 返回成功等同于页面交互通过。未部署，仍按用户要求本地验收后再发布。

## 审查后的修复

详见 [F01–F08 修复与复测](testing/FORMAL_PLATFORM_FIXES_2026-09-07.md)。已完成原文出处校验、下一轮前置条件、模型标识、公开政策、正式文案、长回复续写与记忆交互修复；保留未接通能力的真实状态。

输出预算可通过 `GEMINI_MAX_OUTPUT_TOKENS` 配置，默认 8192、硬上限 16384。短问答最高 4096，计划/清单/续写使用配置上限。长度结束显示待续写，只有最新且来源仍有效的未完成回答可继续；每次续写单独记录实际用量，不会自动发起连续付费调用。

## 未开放接口的展示约定（2026-09-07）

按用户最新要求，充值、团队邀请、真实仿真、设备自动接入与公司模型迭代保留入口，统一显示中性灰“待开放”并禁用相应操作。本轮不建设上述真实集成，也不显示已连接或可付款状态。人工执行、文件回传、CSV 曲线分析、模型对话、Token 用量和数据授权继续可用。

验收页面：`/account/billing`、`/account/members`、`/tools`、`/laboratory`、`/workspace/settings/privacy` 以及研发任务定义。资源卡片不打开未开放的配置表单，任务中的真实仿真与设备自动执行选项不可选择。相关前端检查和实际页面复查通过，未部署。
