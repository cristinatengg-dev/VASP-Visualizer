# EliangMat AI：F01–F08 修复与复测

2026-09-07。本次针对 [正式构建审查](FORMAL_PLATFORM_AUDIT_2026-09-07.md) 的八项可复现缺陷进行修复。仅更新本机，未提交、推送或部署。原审查文档保留作为修复前基线。

## 逐项结果

| 编号 | 修复后的行为 | 验证 |
| --- | --- | --- |
| F01 引用错误 | 只有整段能逐字匹配获准来源的摘录保留出处，统一展示为原文引用块；解释或机理不能借用目标来源。流式传输暂不展示未核对来源编号，旧回复读取时也校验。来源撤回仍使依赖回复失效 | 错误机理、数值改写、越界编号、分片流、旧消息和来源撤回测试通过。真实 Gemini 原文引用核对成功，另起分析段说明目标不是测量 |
| F02 模型误标 | 项目和账号按所选模型与授权状态显示；本地检索显示在账号空间处理，不显示 Gemini 已启用 | Computer Use 切换账号和项目模型，项目页显示“账号记忆检索 · 在账号空间处理”，恢复 Gemini 后显示外部推理 |
| F03 下一轮条件 | 前后端共用路线确认、数据复核与工况可比性检查；不满足时禁用起草并给出补充入口 | 25°C / 315 MPa 不能推进 200°C / 300 MPa 目标；页面与后端 409 原因一致。通过质量复核的可比记录可起草；不代表已有充分重复实验或材料达标 |
| F04 正式文案 | 正式候选页不再提示演练性能；记忆说明改为账号空间；CSV 说明改为上传曲线分析；登录和政策联系入口显示 EliangMat AI | 页面实际检查通过；真实虚构 QA 数据仍保留显著标记。客服仍使用已配置可识别的原邮箱，不杜撰新邮箱 |
| F05 Cookie 路由 | 未登录可直接打开 Cookie 政策，三个公开政策可互相跳转 | Computer Use 在 `localhost:4317` 匿名访问并切换通过，无登录拦截 |
| F06 说明不一致 | 中文说明对应当前账号记忆、项目排除、私密/优化、外部模型确认和供应商 Token 用量；取消不存在的套餐与配额规则。Cookie 说明对应 HttpOnly、7 天会话和项目偏好存储 | 构建与页面阅读通过；逐项对照当前代码，不宣称充值或设备已经上线 |
| F07 回复截断 | 有界输出预算；长度结束显示“待续写”，最新有效回复可点击继续，保留上下文与输入草稿。续写同样受授权、来源撤回、幂等和用量记录约束 | 真实 Gemini 二十项清单完整返回；隔离正式模式 UI 截断→继续→刷新通过；两次点击形成两次正常调用，幂等重试不会额外调用 |
| F08 记忆展开 | 切换账号/项目来源保留展开状态和筛选关键词 | Computer Use 来回切换后 `open=true`，关键词仍为 `QA`，范围和筛选结果正确 |

引用校验属于来源对应检查，不是科学真实性或推论成立性验证；模型分析依然需要研发人员结合原件和实验核验。完整匹配无法确认时移除出处标记，不伪造“引用通过”。

## 验证记录

- `npm run test:platform`：89/89，通过；含 6 项新增行为回归。
- `node scripts/platform/audit-http.cjs`：95 项正式模式 HTTP 检查通过；隔离临时存储、替代短信投递函数，没有真实短信或模型请求。覆盖身份隔离、人工执行、CSV 失败重试、测量与复核、下一轮幂等、记忆和资料权限。
- `npm run build`（含 TypeScript）及本次修改页面 ESLint：通过。构建保留此前大型可视化 chunk 的体积提示；非本次修复引入的编译失败。
- 真实 Gemini 复测全部只使用虚构 QA 记录：工况说明 5.4 秒（1966 输入 / 1519 输出）；完整二十项清单 6.7 秒（1149 / 2163）；原文核对 5.9 秒（2134 / 2236）。输出 Token 包含供应商统计，不等于可见正文长度。
- 长度截断 UI 使用本地替代模型验收，明确非真实模型性能证明：待续写状态、继续按钮、上下文衔接、输入草稿保留、刷新后记录持久化通过，模型调用计数为 2。临时服务和数据已销毁。
- 浏览器错误日志复查为空；已实际查看回复排版、原文块和中性颜色。
- 临时 UI 实例最初与主预览共用同主机 Cookie，覆盖了主预览的浏览器会话。已用已知虚构账号恢复会话，原项目和资料完整，关闭一次性恢复服务及临时实例。后续临时 UI 实例使用不同主机名，不能仅以不同端口隔离 Cookie。
- 主预览留在 `http://127.0.0.1:4317/workspace/results`。账号模型恢复本地检索，QA 项目保留原 Gemini 选择，数据仍为私密。

测试日志见 `artifacts/platform-fixes-tests-2026-09-07.txt`；HTTP 机器报告见 `artifacts/platform-http-audit-2026-09-07.json`。

## 短信与其他接入项

本机没有原可用短信配置；已只读核对此前服务器，将五项必要腾讯云短信参数复用到 `.dev/phone-auth.env`。配置文件为 0600 且被 Git 忽略，没有复制线上用户、会话或研究数据。重启预览后健康检查 200、无效号码校验 400，未发送真实短信。已向用户请求一个由其控制的测试号码，待做送达验证；尚不能称为真实登录闭环已验收。

| 项目 | 本次状态 | 仍需完成 |
| --- | --- | --- |
| 短信 | 配置已载入，正式校验正常 | 用户控制的收件号码与实际投递/登录/同号恢复验收 |
| 支付充值 | 本机及已知服务器未发现可复用商户配置；未开通状态保留 | 商户通道与费率，订单/回调/幂等入账/扣费/对账实现与真实验收 |
| 多成员协作 | 权限隔离保留，真实邀请尚未实现 | 邀请接受、撤销、角色变更与多人协作测试 |
| DFT/CALPHAD/MD/CFD 与设备 | 未连接真实引擎或仪器，本次未运行实际实验 | 确定首个软件和设备，提供合法引擎/数据库或工作站接口，完成任务→原件→复核闭环 |
| 自有模型迭代 | 授权和候选导出可用，训练未连接 | 数据集版本、训练/评测任务、模型发布与回退 |
| 专利/全文 | 公共题录与有权资料导入现有能力保留 | 机构数据源权限、全文许可和增量接入；Unpaywall 尚无邮箱配置 |
| 企业容量 | 仍为单进程 JSON 存储，项目/文献/状态容量限制未解除 | 数据库和对象存储迁移、备份恢复、并发与容量验收 |

这些是尚未完成的建设工作，不纳入“八项缺陷已修复”的完成范围，也不能以正常状态页或模拟测试替代真实验收。

## 用户测试路径

1. 结果页：当前虚构 25°C 数据下，“生成下一轮规则草案”禁用，原因和“补充可比实验记录”入口清晰。
2. 协作页：查看最新原文摘录与分析，展开历史来源；到项目模型切换本地检索，返回后检查模型标签。
3. 对话页：选择需要的模型后提交二十项检查点等长清单；实际达到长度上限时有“继续生成”。该操作会形成新调用并记录供应商用量。
4. 账号与用量 → 自动记忆：展开管理，输入关键词并切换来源，展开状态和关键词应保留。
5. 新的未登录浏览器打开 `/cookie-policy`，可继续阅读服务条款和隐私政策。

生产部署仍等待用户本机测试通过后另行确认；当前线上未变更。

## 说明文本依据

服务和数据用途说明以当前实现为准。处理用途、保存方式和用户控制入口的完整性参考[《中华人民共和国个人信息保护法》公开文本](https://www.npc.gov.cn/npc/c2/c30834/202108/t20210820_313088.html)；本次完成的是产品说明一致性修改，不代表跨境处理、供应商合同等上线事项已通过专项审核。
