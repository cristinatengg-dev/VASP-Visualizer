# Copy to .dev/model.env (mode 600). This file is ignored by Git and read only by the backend.
# Reuse the existing gateway; choose an available Gemini text model from its model list.
GEMINI_BASE_URL=https://api.aipaibox.com/v1
GEMINI_API_KEY=
GEMINI_TEXT_MODEL=gemini-3-flash
